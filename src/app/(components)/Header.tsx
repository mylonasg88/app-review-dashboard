import { AppBar, Typography } from "@mui/material";
import React from "react";

function Header() {
  return (
    <AppBar>
      <Typography variant="h3">App Submissions</Typography>
    </AppBar>
  );
}

export default Header;
